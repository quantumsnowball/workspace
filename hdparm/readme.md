# hdparm

These are helpers services or rules that may help unused hdd drives to save energy.
However, wake it up from standby mode takes a few seconds which will reduce system performance.
The best is not to use it, install a physical switch at the PC cases. 
Use physical switch to power it off complete, so the system never detect it in the first place.
