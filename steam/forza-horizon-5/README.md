# Installation

* I tried steamrip version on Linux
* must add it as a non-steam game and run it via steam, otherwise will crash steamclient error
* `WINEDLLOVERRIDES="winmm=n,b"` may be necessary to run successfully
* try launch a few times, and toggle windows, may crash a few times but finally will launch splash screen
* first time launch need to login as xbox account
* anyways, ffb still not working, no spring force just like FH6
